# AnnaMithra Website

Open `index.html` to preview. Update company contact details in `assets/js/main.js` and shared header/footer strings in the HTML files. Product data is stored in `assets/js/product-data.js` and `data/products.json`.

This is an enquiry catalogue, not e-commerce.
